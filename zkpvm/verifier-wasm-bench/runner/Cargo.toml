[package]
name = "vwb-runner"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
# Same major as the installed wasmtime CLI (45.0.2) — report the exact
# resolved version from `cargo tree` in the spike write-up.
wasmtime = "45"
anyhow = "1"
