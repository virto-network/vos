[package]
name = "vwb-runner"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
# Pinned to the newest wasmtime whose MSRV matches the project's rust-toolchain
# (1.88.0-nightly). Report the exact resolved version from `cargo tree` in the
# spike write-up.
wasmtime = "38"
anyhow = "1"
