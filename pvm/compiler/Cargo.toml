[package]
name = "vos-pvm-compiler"
version.workspace = true
edition.workspace = true
license = "Apache-2.0"
description = "RISC-V ELF to VOS PVM compiler"

[dependencies]
vos-pvm = { path = "../runtime" }
vos-pvm-program = { path = "../program" }
scale = { package = "vos-pvm-codec", path = "../codec" }
thiserror = "2"
tracing = "0.1"
tracing-test = "0.2"

[dev-dependencies]
proptest = "1"
