[package]
name = "registry-client"
description = "Host-side client over VosNode for the hyperspace registry. Wraps registry's wire types in a typed API."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
registry = { path = "../registry", default-features = false }
vos = { path = "../vos", default-features = false, features = ["std"] }
