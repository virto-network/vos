[package]
name = "vos-abi"
description = "VOS ABI — JAR-aligned hostcall interface for VOS actors (zero deps)"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
guest = ["alloc"]
alloc = []
