[build]
# Remap the complete repository, including the sibling `vos` path dependency.
# `-Zremap-cwd-prefix` alone covers only this service package.
rustc-wrapper = "./rustc-remap.sh"

[target.riscv64em-javm]
rustflags = [
    "-Zunstable-options",
    "-Zcrate-attr=no_std",
    "-Zcrate-attr=no_main",
    "-Aduplicate-macro-attributes",
    "-Aunused-attributes",
    # Canonical service identity must not depend on its checkout directory.
    "-Zremap-cwd-prefix=.",
]

[alias]
actor = "rustc --lib --crate-type bin -Zbuild-std=core,alloc,compiler_builtins -Zbuild-std-features=compiler-builtins-mem --release --target riscv64em-javm.json"

[unstable]
json-target-spec = true
