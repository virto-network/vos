[workspace]

[package]
name = "clerk-refine-bench"
version = "0.1.0"
edition = "2024"

[dependencies]
vos = { path = "../../../crates/vos", default-features = false, features = ["macros", "service"] }
cipher-clerk = { path = "/home/daniel/src/virto/cipher-clerk", default-features = false }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
