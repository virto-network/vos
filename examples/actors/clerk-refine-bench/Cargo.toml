[workspace]

[package]
name = "clerk-refine-bench"
version = "0.1.0"
edition = "2024"

[dependencies]
cipher-clerk = { path = "/home/daniel/src/virto/cipher-clerk", default-features = false }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
