[workspace]

[package]
name = "counter"
version = "0.1.0"
edition = "2024"

[dependencies]
vos = { path = "../../../crates/vos", default-features = false, features = ["macros", "pvm"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
