[workspace]

[package]
name = "counter"
version = "0.1.0"
edition = "2024"

[dependencies]
vos-actors = { path = "../../crates/vos-actors", features = ["vos-actors-macros", "guest"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
