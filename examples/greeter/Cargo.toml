[workspace]

[package]
name = "greeter"
version = "0.1.0"
edition = "2024"

[dependencies]
pvx-actors = { path = "../../crates/pvx-actors", features = ["pvx-actors-macros", "guest"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
