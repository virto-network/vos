# Hyperspace demo. With `hyperspace` set, vosx auto-spawns a
# registry replica at ServiceId::REGISTRY and announces every
# declared agent into it on startup. Two vosx processes pointed
# at this same file converge on a shared directory.
#
#   vosx start examples/space-hyperspace.toml \
#     --data-dir /tmp/vosx-a --listen /ip4/127.0.0.1/tcp/4811
#   vosx start examples/space-hyperspace.toml \
#     --data-dir /tmp/vosx-b --connect /ip4/127.0.0.1/tcp/4811

space = "hyperspace-demo"
version = "0.1.0"
hyperspace = "kunekt-test"
# vosx loads this elf for the registry actor. Resolved relative
# to the manifest directory. Future cycles will embed the actor.
registry_blob = "../crates/actors/registry/target/riscv64em-javm/release/registry-actor.elf"

[[agent]]
name = "counter"
path = "actors/crdt-counter/target/riscv64em-javm/release/crdt-counter.elf"
consistency = "crdt"
provides = ["counter", "demo-service"]
on_start = [
  { msg = "inc", args = { tag = 1 } },
]
