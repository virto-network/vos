[workspace]

[package]
name = "space-authority"
description = "Canonical VOS service role-authority actor"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"

[features]
default = ["bin"]
bin = []
# Test-only code-identity delta used by the physical UpgradeActor gate. The
# canonical authority artifact is always built without this feature.
upgrade-fixture = []

[lib]
crate-type = ["rlib", "cdylib"]

[target.'cfg(target_arch = "riscv64")'.dependencies]
vos = { path = "../../vos", default-features = false, features = ["macros", "service"] }
ed25519-dalek = { version = "2", default-features = false }

[target.'cfg(not(target_arch = "riscv64"))'.dependencies]
vos = { path = "../../vos", default-features = false, features = ["macros", "extension"] }
ed25519-dalek = { version = "2", default-features = false, features = ["rand_core"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"

[dev-dependencies]
vos = { path = "../../vos", default-features = false, features = ["macros", "extension", "std", "storage", "network"] }
